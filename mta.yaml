_schema-version: 3.3.0
ID: incident-management-demo
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
  deploy_mode: html5-repo
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: incident-management-demo-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: incident-management-demo-auth
      - name: incident-management-demo-db
      - name: incident-management-demo-destination

  - name: incident-management-demo-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: incident-management-demo-db

  - name: incident-management-demo-app-deployer
    type: com.sap.application.content
    path: gen
    requires:
      - name: incident-management-demo-auth
      - name: srv-api
      - name: incident-management-demo-html5-repo-host
        parameters:
          content-target: true
          config:
            HTML5Runtime_enabled: true
    build-parameters:
      build-result: app/
      requires:
        - name: incidentmanagementdemoincidents_man_demo
          artifacts:
            - incidents.zip
          target-path: app/

  - name: incidentmanagementdemoincidents_man_demo
    type: html5
    path: app/incidents_man_demo
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []

  - name: incident-management-demo-destinations
    type: com.sap.application.content
    requires:
      - name: incident-management-demo-auth
        parameters:
          service-key:
            name: incident-management-demo-auth-key
      - name: incident-management-demo-html5-repo-host
        parameters:
          service-key:
            name: incident-management-demo-html5-repo-host-key
      - name: srv-api
      - name: incident-management-demo-destination
        parameters:
          content-target: true
    build-parameters:
      no-source: true
    parameters:
      content:
        instance:
          existing_destinations_policy: update
          destinations:
            - Name: incident-management-demo-html5-repository
              ServiceInstanceName: incident-management-demo-html5-repo-host
              ServiceKeyName: incident-management-demo-html5-repo-host-key
              sap.cloud.service: incidentmanagementdemo.service
            - Name: incident-management-demo-auth
              Authentication: OAuth2UserTokenExchange
              ServiceInstanceName: incident-management-demo-auth
              ServiceKeyName: incident-management-demo-auth-key
              sap.cloud.service: incidentmanagementdemo.service

resources:
  - name: incident-management-demo-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: incident-management-demo-${org}-${space}
        tenant-mode: dedicated
        oauth2-configuration:
          credential-types:
            - "binding-secret"
            - "x509"
        role-collections:
          - name: 'support (incident-management-demo ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.support'
          - name: 'admin (incident-management-demo ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.admin'
  - name: incident-management-demo-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: incident-management-demo-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            existing_destinations_policy: update
            destinations:
              - Name: srv-api
                URL: ~{srv-api/srv-url}
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet
                HTML5.ForwardAuthToken: true
                HTML5.DynamicDestination: true
    requires:
      - name: srv-api

  - name: incident-management-demo-html5-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host
